info:
  app:
    name: Capela API
    version: @project.version@
    description: API para Inscrições de Eventos Espíritas.

server:
  port: 8080
  connection-timeout: 1800

management:
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /manager

spring:
  redis:
    host: localhost
    port: 6379
  datasource:
    driverClassName: org.postgresql.Driver
    hikari:
      auto-commit: false
    password: postgres
    username: postgres
    url: jdbc:postgresql://localhost:5432/capela?useSSL=false
  cache:
    type: redis
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true